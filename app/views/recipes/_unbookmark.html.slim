= link_to bookmark_path(current_user.bookmarks.find_by(recipe_id: recipe.id)), id: "unbookmark-button-for-#{recipe.id}", data: { turbo_method: :delete }, class: 'bookmark-button bg-secondary text-white rounded-full' do
  i.i-lucide-bookmark-check.bookmark-icon